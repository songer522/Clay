# Levels 9–11 Widescreen Modernization Plan

**Goal:** Bring Stormy / Aquarium / Final Run to the same widescreen state as levels 1–8,
covering the code paths those levels never exercised (the level 9 ambient rain and
lightning layer, level 10's underwater physics, and the level 11 `BossFinal` train).

**Architecture:** Keep `GameCollisionRectForObject` the single AABB source. Every
widescreen formula must reproduce the legacy numbers exactly at 480×320 phone and
1024×768 iPad, so it is a no-op at the authored design size and diverges only where the
screen is genuinely bigger. Derive from live `winSize` using the
`widthScale = max(1, winWidth/480)` idiom established in levels 3–8.

**Spec:** `docs/superpowers/specs/2026-09-05-levels91011-widescreen-design.md`
**Findings:** `docs/superpowers/specs/2026-09-05-levels91011-widescreen-findings.md`

**Verification:** No XCTest target, and per the levels 2–8 plans one must not be added.
Verification is the debug overlay plus playthrough on iPhone and iPad. Build with
`DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer` — `xcode-select` points at
CommandLineTools, so bare `xcodebuild` fails. There is no iOS 18.6 simulator runtime; the
app's deployment target means only iOS 26+ simulators are eligible.

---

## File map

| File | Responsibility |
|---|---|
| `Clay/Classes/Game/Bosses/BossFinal.m` | Un-swap the entrance constants; `FinalBossStageX()` right-anchoring; scaled door box |
| `Clay/Classes/Game/Effects/Raindrop.m` | Width-derived spawn span; `MULTIPLIERY`-gated world y |
| `Clay/Classes/Game/Effects/Lightning.m` | Width-derived spawn span; `MULTIPLIERY`-gated parallax y |
| `Clay/Classes/Game/Effects/RainyLevelEffects.m` | Scaled rain-behind-Tim offset |
| `Clay/Classes/Game/GameObjects/GameObject.m` | `GameObjectWidthScale()` across L9–11; scaled squirrel-nut / squid-ink / gargoyle boxes |
| `Clay/Classes/Game/Other/Projectile.m` | Scaled dark bomb / grape boxes; missing `break` |
| `Clay/Classes/Game/PlayerActions/PlayerActionSpin.m` | Device-correct floor test |
| `Clay/Classes/Game/PlayerActions/PlayerActionSlowTime.m` | Scaled slow-time sprite offset |

---

### Task 1: Offline map audit — DONE

- [x] Decoded all fifteen shipped variants; confirmed obstacle names, pit/ledge meta vs.
      art consistency, and layer lists. Results in the findings doc's "Verified clean"
      section so the next pass does not repeat it.

### Task 2: P0 functional breakage — DONE, unverified in play

- [x] Level 11 boss entrance device constants were swapped; swapped back.
- [x] Level 9 rain and lightning spawn spans derived from live `winSize`; the two
      unconditional `2.4` literals gated on `IS_IPAD`.
- [x] Level 10 `SPIN_PLAYER_GROUND_Y` scaled by `MULTIPLIERY`.

### Task 3: Per-level widescreen passes — code done, feel unverified

- [x] **L9 Stormy** — umbrella, paper plane and squirrel triggers on `widthScale`;
      squirrel-nut spawn offset and box scaled; rain-behind-Tim offset scaled.
- [x] **L10 Aquarium** — pufferfish, jimWater, angler, tronika and squid distances and
      chase speeds on `widthScale`; squid ink box scaled.
- [x] **L11 Final** — spike, bomb and gargoyle triggers on `widthScale`; gargoyle,
      bomb, grape and door boxes scaled; train stage positions right-anchored;
      slow-time sprite offset scaled.
- [x] Hand-tuned per-device pairs (spike stop height, bat arc) identified and
      **kept**, with comments explaining why they are not scaling bugs.
- [ ] **Play each level on phone and iPad with the overlay on** and lock the constants.
      See "Still open" in the findings doc for the specific checks.

### Task 4: Cleanups — DONE

- [x] `Projectile.m` squirrel-nut case missing `break`.
- [x] Dead `BOSS_FINAL_MAX_TRAIN_X`.

### Task 5: Unreachable content — reported, no map edits

- [x] `finalBossSpawn` / `finalBossDies` absent from every level 11 variant; the
      `BossFinalJim` shadow chase never spawns and the boss is never killed. Documented
      in the findings, including why a tile alone would not fix it.
- [x] Unused `objects.plist` entries, the empty `WaterLevelEffects`, and the
      `timed_normal` bumper tagging inconsistency recorded.

### Task 6: Docs — DONE

- [x] Design, plan, findings trio.
- [x] README corrected: `BossFinalJim` is unreachable in shipped maps; the level 11 boss
      is the `BossFinal` train.
